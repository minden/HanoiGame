class Main {

    /** Initializes a new game and starts it. */
    function void main() {
        var HanoiGame game;

        let game = HanoiGame.new();
        do game.run();
        do game.dispose();

        return;
    }
}
